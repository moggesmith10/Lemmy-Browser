import { StyleSheet } from "react-native";

export const TextPostStyles = StyleSheet.create({
    header:{
        color: "lightgrey"
    },
    title:{
        color: "white",
        fontSize: 20
    }
});
